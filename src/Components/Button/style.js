import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonStyle: {
    width: 300,
    height: 48,
    alignSelf: 'center',
    justifyContent: 'center',
    alignItems: 'center',
    borderWidth: 1,
    borderColor: 'green',
    borderRadius: 12,
    zIndex: 1,
  },
  textStyle: {
    fontSize: 18,
    color: 'green',
    fontWeight: 'bold',
  },
});
