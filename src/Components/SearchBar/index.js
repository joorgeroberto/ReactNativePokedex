import React from 'react';
import {TouchableOpacity, Text, Image, View} from 'react-native';
import {Container} from './style';
import {returnId, returnType} from '../../Utils';

const SearchBar = ({onPress, style, data}) =>
  <Container />
;

export {SearchBar};
